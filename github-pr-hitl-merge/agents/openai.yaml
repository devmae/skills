interface:
  display_name: "GitHub PR HITL Merge"
  short_description: "Summarize a PR, then merge it"
  default_prompt: "Use $github-pr-hitl-merge to post a human-readable HITL summary on this Pull Request, verify the same snapshot, then merge and clean it up."
