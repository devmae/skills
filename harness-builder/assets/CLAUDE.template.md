# <PROJECT_NAME> — 에이전트 온보딩 (CLAUDE.md)

> 모든 세션 시작 시 이 문서를 읽어라. 이것은 1,000페이지짜리 매뉴얼이 아니라 지도다.

## 프로젝트 컨텍스트
<한 문단: 이 프로젝트가 무엇인지, 누가 쓰는지, 현재 성숙도>

## 스택 & 구조
- 언어/런타임: <감지됨>
- 패키지 매니저: <감지됨>
- 핵심 디렉터리: <git ls-files 기준 실제 경로>
- 진입점: <실제 파일>

## 코딩 컨벤션
- <린트/포맷 규칙, 네이밍, import 순서 — 실제 설정 파일 기반>
- 커밋 전 실행: <lint cmd>, <typecheck cmd>, <test cmd>

## 아키텍처 결정
- 의존성 흐름(이 순서를 지킬 것): Types → Config → Repo → Service → Runtime → UI
- <주요 결정과 그 "이유">

## 여기서 일하는 방식
- 스프린트당 기능 하나, 끝날 때마다 커밋.
- 파일 경로나 API를 절대 지어내지 말 것 — 먼저 grep.
- 기능이 통과될 때마다 harness/features.json 업데이트.

## 현재 진행 중
<지금 작업 중인 내용; 우선순위가 가장 높은 실패 기능을 반영>
