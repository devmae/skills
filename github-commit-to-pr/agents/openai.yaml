interface:
  display_name: "GitHub Commit to PR"
  short_description: "Run Matt review, then create a PR"
  default_prompt: "Use $github-commit-to-pr to run mattpocock:code-review, push the exact reviewed SHA, and create a concise Pull Request."
