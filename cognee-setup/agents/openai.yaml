interface:
  display_name: "Cognee Setup"
  short_description: "user scope 전용 keyless Cognee 연결"
  default_prompt: "Use $cognee-setup to connect this project to Cognee in user scope over Tailscale without an API key."
